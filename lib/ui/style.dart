
import 'package:flutter/material.dart';

// Default padding for UI elements
const EdgeInsets DEFAULT_PADDING = EdgeInsets.all(16.0);
const EdgeInsets DEFAULT_CARD_PADDING = EdgeInsets.all(12.0);

const SizedBox DEFAULT_WIDGET_VERTICAL_SPACE = SizedBox(height: 16);

// Style for section titles in settings
const TextStyle CONFIG_SECTION_TITLE_STYLE = TextStyle(
  fontSize: 18,
  fontWeight: FontWeight.bold,
);